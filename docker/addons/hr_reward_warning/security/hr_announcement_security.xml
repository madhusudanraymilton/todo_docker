<?xml version="1.0" encoding="utf-8"?>
<odoo>

    <!-- HR Announcement Multi Company rule (Your existing rule) -->
    <record id="hr_announcement_rule_company" model="ir.rule">
        <field name="name">HR Announcement Multi Company</field>
        <field name="model_id" ref="model_hr_announcement"/>
        <field eval="True" name="global"/>
        <field name="domain_force">
            ['|',('company_id','=',False),('company_id','child_of',[user.company_id.id])]
        </field>
    </record>

    <record id="rule_portal_read_announcement_attachment" model="ir.rule">
        <field name="name">Portal: Read Announcement Attachments</field>
        <field name="model_id" ref="base.model_ir_attachment"/>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="domain_force">
            [
                ('res_model', '=', 'hr.announcement'),
                ('company_id', '=', user.company_id.id),
            ]
        </field>
        <field name="perm_read" eval="True"/>
    </record>


</odoo>