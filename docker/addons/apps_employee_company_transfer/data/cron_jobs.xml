<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <record id="ir_cron_check_temporary_transfer_expiry" model="ir.cron">
            <field name="name">Check Temporary Transfer Expiry</field>
            <field name="model_id" ref="model_hr_company_transfer"/>
            <field name="state">code</field>
            <field name="code">model._cron_check_temporary_transfer_expiry()</field>
            <field name="interval_number">1</field>
            <field name="interval_type">days</field>
            <field name="active">True</field>
            <field name="user_id" ref="base.user_root"/>
        </record>

    </data>
</odoo>