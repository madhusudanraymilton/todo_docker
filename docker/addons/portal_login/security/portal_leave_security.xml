<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <!--
        SECURITY RULES FOR PORTAL USERS
        - Portal users can ONLY access their OWN leave data
        - Portal users can READ all active employees (for delegation dropdown)
        - Portal users CANNOT modify employee records
    -->

    <!-- Portal users can ONLY see their own leave requests -->
    <record id="portal_leave_rule_strict" model="ir.rule">
        <field name="name">Portal User: Own Leaves Only (Strict)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users can ONLY view their own leave allocations (READ ONLY) -->
    <record id="portal_leave_allocation_rule_strict" model="ir.rule">
        <field name="name">Portal User: Own Allocations Only (Read Only)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave_allocation"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- FIXED: Portal users can see ALL ACTIVE employees (for delegation dropdown) - READ ONLY -->
    <record id="portal_employee_rule_read_all" model="ir.rule">
        <field name="name">Portal User: Read All Active Employees</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="domain_force">[('active', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users can see all ACTIVE leave types (READ ONLY) -->
    <record id="portal_leave_type_rule_strict" model="ir.rule">
        <field name="name">Portal User: All Active Leave Types (Read Only)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave_type"/>
        <field name="domain_force">[('active', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <record id="portal_employee_barcode_rule_strict" model="ir.rule">
        <field name="name">Portal User: Barcode (Read Only)</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="domain_force">[('user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>


    <!-- Portal users can see their own leaves OR leaves where they are team leader -->
    <record id="portal_leave_rule_strict" model="ir.rule">
        <field name="name">Portal User: Own Leaves OR Team Leaves (Team Leader)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave"/>
        <field name="domain_force">['|', ('employee_id.user_id', '=', user.id), ('employee_id.portal_team_leader_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="True"/>
        <field name="perm_create" eval="True"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users can ONLY view their own leave allocations (READ ONLY) -->
    <record id="portal_leave_allocation_rule_strict" model="ir.rule">
        <field name="name">Portal User: Own Allocations Only (Read Only)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave_allocation"/>
        <field name="domain_force">[('employee_id.user_id', '=', user.id)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users can see ALL ACTIVE employees (for delegation dropdown) - READ ONLY -->
    <record id="portal_employee_rule_read_all" model="ir.rule">
        <field name="name">Portal User: Read All Active Employees</field>
        <field name="model_id" ref="hr.model_hr_employee"/>
        <field name="domain_force">[('active', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

    <!-- Portal users can see all ACTIVE leave types (READ ONLY) -->
    <record id="portal_leave_type_rule_strict" model="ir.rule">
        <field name="name">Portal User: All Active Leave Types (Read Only)</field>
        <field name="model_id" ref="hr_holidays.model_hr_leave_type"/>
        <field name="domain_force">[('active', '=', True)]</field>
        <field name="groups" eval="[(4, ref('base.group_portal'))]"/>
        <field name="perm_read" eval="True"/>
        <field name="perm_write" eval="False"/>
        <field name="perm_create" eval="False"/>
        <field name="perm_unlink" eval="False"/>
    </record>

</odoo>