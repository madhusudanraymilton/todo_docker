<!-- <?xml version="1.0" encoding="utf-8"?>
<odoo>

    <template id="report_payslip_safe_contract_date"
              inherit_id="hr_payroll.report_payslip">

        <xpath expr="//span[contains(@t-if, 'contract_date_start')]"
               position="replace">

            <span t-if="o.version_id
                        and o.version_id.contract_date_start
                        and o.date_from &lt; o.version_id.contract_date_start"
                  t-field="o.version_id.contract_date_start"/>

        </xpath>

    </template>

</odoo> -->
